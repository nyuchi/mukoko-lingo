[tool.ruff]
line-length = 150
target-version = "py312"
exclude = ["node_modules", "web", ".venv-ci-test"]

[tool.ruff.lint]
select = ["E", "F", "W"]

[tool.pytest.ini_options]
testpaths = ["api/analytics/tests"]
